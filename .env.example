# printed by `npm run seed` in edge-analyst (tenant acme)
CAMADA_KEY=tok-acme.snap-acme
CAMADA_INGEST_URL=http://localhost:8787
CAMADA_SNAPSHOT_URL=http://localhost:8787/snapshot
# dev only: lets `curl -H 'X-Forwarded-For: …'` exercise blocking through localhost
CAMADA_TRUSTED_PROXY=hops:1
